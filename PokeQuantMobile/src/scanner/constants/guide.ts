// Shared geometry for the on-screen card guide and the guide-fallback crop.
export const GUIDE_ASPECT = 2.5 / 3.5;
export const GUIDE_WIDTH_FRACTION = 0.85;
// Vertical center of the guide frame as a fraction of screen height.
// Lifted above center so the bottom controls don't cover the card.
export const GUIDE_CENTER_Y = 0.4;
